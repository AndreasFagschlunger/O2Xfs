dependencies {
    compile project(':at.o2xfs.emv'),
            project(':at.o2xfs.xfs.service')
    testCompile libraries.junit
}

publishing {
    publications {
        mavenJava(MavenPublication) {
            groupId 'at.o2xfs'
            artifactId 'o2xfs-emv-pinpad-impl-xfs'
            version version

            from components.java
        }
    }
}